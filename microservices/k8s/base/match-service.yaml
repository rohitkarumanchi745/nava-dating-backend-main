apiVersion: apps/v1
kind: Deployment
metadata:
  name: match-service
  namespace: nava
  labels:
    app: match-service
spec:
  replicas: 3
  selector:
    matchLabels:
      app: match-service
  template:
    metadata:
      labels:
        app: match-service
    spec:
      containers:
        - name: match-service
          image: nava/match-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8003
          envFrom:
            - configMapRef:
                name: nava-config
            - secretRef:
                name: nava-secrets
          resources:
            requests:
              memory: "256Mi"
              cpu: "200m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8003
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8003
            initialDelaySeconds: 5
            periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: match-service
  namespace: nava
spec:
  selector:
    app: match-service
  ports:
    - port: 8003
      targetPort: 8003
