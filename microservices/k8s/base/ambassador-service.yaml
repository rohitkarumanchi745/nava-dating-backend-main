apiVersion: apps/v1
kind: Deployment
metadata:
  name: ambassador-service
  namespace: nava
  labels:
    app: ambassador-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ambassador-service
  template:
    metadata:
      labels:
        app: ambassador-service
    spec:
      containers:
        - name: ambassador-service
          image: nava/ambassador-service:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8006
          envFrom:
            - configMapRef:
                name: nava-config
            - secretRef:
                name: nava-secrets
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 10
            periodSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8006
            initialDelaySeconds: 5
            periodSeconds: 5

---
apiVersion: v1
kind: Service
metadata:
  name: ambassador-service
  namespace: nava
spec:
  selector:
    app: ambassador-service
  ports:
    - port: 8006
      targetPort: 8006
